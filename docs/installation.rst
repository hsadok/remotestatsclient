.. highlight:: shell

============
Installation
============

At the command line::

    $ pip install remotestatsclient

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv remotestatsclient
    $ pip install remotestatsclient
